import GeneralPage from "@/app/components/GeneralPage";
import {SafeAreaProvider} from "react-native-safe-area-context";

export default function Finance() {
    return (
        <GeneralPage title={'Finance'}>
            <SafeAreaProvider>

            </SafeAreaProvider>
        </GeneralPage>
    )
}
